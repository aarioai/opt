#!/bin/sh
set -eu

if [ -x "../lib/aa-posix-lib.sh" ]; then
    . ../lib/aa-posix-lib.sh
else
    . /opt/aa/lib/aa-posix-lib.sh
fi

main(){
    if [ $# -lt 2 ]; then PanicUsage "$0 <user[:group]> <dir> [dir]..."; fi
    ChownOrMkdir "$@"
}

main "$@"